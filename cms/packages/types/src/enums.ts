export enum EStateType {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  BOTH = 'both',
}

export enum EContentType {
  CONTENT_ITEMS = 'content-items',
  PAGES = 'pages',
  PAGE_ITEMS = 'page-items',
}
