// Re-export Puck's own types so consumers import from a single package
export type {
  Config as PuckConfig,
  ComponentConfig as PuckComponentConfig,
  Data as PuckData,
  ComponentData as PuckComponentData,
  Fields,
  Field,
} from '@measured/puck';
